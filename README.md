# recent-backend

## Install

Use `pnpm` to install dependencies and run the worker locally.

```bash
pnpm install
pnpm run start
```

Replace `pnpm run start` with `wrangler dev` if you prefer to call Wrangler directly.
# recent-backend